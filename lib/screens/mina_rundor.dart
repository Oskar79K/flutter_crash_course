import 'package:flutter/material.dart';

class MinaRundor extends StatelessWidget {
  const MinaRundor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Mina rundor"),
    );
  }
}
