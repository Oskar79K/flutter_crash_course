import 'package:flutter/material.dart';

class Speltips extends StatelessWidget {
  const Speltips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Speltips"),
    );
  }
}
